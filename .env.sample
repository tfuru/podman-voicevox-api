# Copy this file to .env and fill in your actual values.

# A strong, randomly generated key for administering the API
ADMIN_API_KEY=change_me_to_a_secure_random_key
PORT=8080

# (Optional) Specify the target platform for the containers (e.g., linux/amd64, linux/arm64).
# Useful for running on different architectures like Apple Silicon Macs.
#PLATFORM=linux/amd64
